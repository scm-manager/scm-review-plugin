[users]
trillian = secret, user
rr = secret, rrr
dent = secret, admin
slarti = secret, push

[roles]
admin = *
user = repository:read:*
rrr = "repository:read,readPullRequest:*"
push = repository:*:repo_ID
